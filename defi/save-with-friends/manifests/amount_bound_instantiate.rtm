CALL_METHOD
	Address("${payer_account}")
	"lock_fee"
	Decimal("100");


CALL_FUNCTION
	Address("${package_address}")
	"SaveWithFriends"
	"new_amount_bound"
    Decimal("1000")
	Array<Address>(
        Address("${account_1}"), 
        Address("${account_2}"), 
        Address("${account_3}")
    );

CALL_METHOD
	Address("${payer_account}")
	"deposit_batch"
	Expression("ENTIRE_WORKTOP");