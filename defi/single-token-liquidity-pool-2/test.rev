// Revup command file
// Double slash comments are allowed and ignored.
// Blank lines are also ignored.

// reset If revup sees you calling reset, then it clears your _.env_ file.
reset

new-account -> account pubkey privkey
new-account -> account2 pubkey2 privkey2
new-account -> account3 pubkey3 privkey3

publish . -> package we can allow comments here as well

call-function $package LiquidityPool instantiate_pool 1000,$tokenXRD "LPT" "LP_Token" 10 -> component lp_mint_bage lp_token transient_mint_badge transient_token

set-default-account $account2 $privkey2

call-method $component add_liquidity 1000,$tokenXRD

set-default-account $account $privkey

call-method $component collect_fee 300,$tokenXRD

set-default-account $account3 $privkey3

call-method $component add_liquidity 1000,$tokenXRD

set-default-account $account $privkey

call-method $component collect_fee 500,$tokenXRD

set-default-account $account $privkey

call-method $component remove_liquidity 1000,$lp_token 

set-default-account $account2 $privkey2

call-method $component remove_liquidity 1000,$lp_token 

set-default-account $account3 $privkey3

call-method $component remove_liquidity 869.5652173913,$lp_token 

show $account

show $account2

show $account3